

#if (!requireNamespace("BiocManager", quietly = TRUE))
#    install.packages("BiocManager")
#BiocManager::install("limma")


library(limma)           #???ð?
expFile="merge.txt"      #?????????ļ?
geneFile="diff.txt"      #???????????б??ļ?
setwd("C:\\Users\\17860\\Desktop\\OV\\7.diffGRGexp")     #???ù???Ŀ¼

#??ȡ?????ļ??????????ݽ??д???
rt=read.table(expFile, header=T, sep="\t", check.names=F)
rt=as.matrix(rt)
rownames(rt)=rt[,1]
exp=rt[,2:ncol(rt)]
dimnames=list(rownames(exp),colnames(exp))
data=matrix(as.numeric(as.matrix(exp)),nrow=nrow(exp),dimnames=dimnames)
data=avereps(data)

#??ȡ???????????б??ļ?, ??ȡ?????????ı???��
gene=read.table(geneFile, header=T, sep="\t", check.names=F)
sameGene=intersect(as.vector(gene[,1]), rownames(data))
geneExp=data[sameGene,]

#?????????????ı???��
outTab=rbind(ID=colnames(geneExp),geneExp)
write.table(outTab, file="diffARGexp.tGt", sep="\t", quote=F, col.names=F)


######V